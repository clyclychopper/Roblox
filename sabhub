local Players = game:GetService("Players")
local HttpService = game:GetService("HttpService")
local TeleportService = game:GetService("TeleportService")
local CoreGui = game:GetService("CoreGui")
local UserInputService = game:GetService("UserInputService")

local GITHUB_TOKEN = getgenv().GithubToken or error("❌ Set getgenv().GithubToken first")
local GIST_ID = getgenv().GistID or error("❌ Set getgenv().GistID first")
local GIST_FILENAME = "brainrot_server.json"
local REFRESH_INTERVAL = 5
local USE_MOCK_DATA = false
local lastKnownTimestamp = 0

-- ══════════════════════════════════════════
--  THEME — Pitch Black Modern
-- ══════════════════════════════════════════
local T = {
    Bg          = Color3.fromRGB(8, 8, 8),
    Panel       = Color3.fromRGB(14, 14, 14),
    Card        = Color3.fromRGB(20, 20, 20),
    CardHover   = Color3.fromRGB(26, 26, 26),
    Border      = Color3.fromRGB(32, 32, 32),
    BorderBright= Color3.fromRGB(48, 48, 48),
    Accent      = Color3.fromRGB(255, 255, 255),
    AccentDim   = Color3.fromRGB(180, 180, 180),
    AccentSub   = Color3.fromRGB(100, 100, 100),
    Green       = Color3.fromRGB(74, 222, 128),
    GreenDim    = Color3.fromRGB(34, 120, 60),
    Yellow      = Color3.fromRGB(250, 204, 21),
    Orange      = Color3.fromRGB(251, 146, 60),
    Red         = Color3.fromRGB(248, 113, 113),
    Purple      = Color3.fromRGB(167, 139, 250),
    Blue        = Color3.fromRGB(96, 165, 250),
    Cyan        = Color3.fromRGB(34, 211, 238),
    Text        = Color3.fromRGB(240, 240, 240),
    TextMid     = Color3.fromRGB(160, 160, 160),
    TextDim     = Color3.fromRGB(90, 90, 90),
    Gold        = Color3.fromRGB(251, 191, 36),
    Transparent = Color3.fromRGB(0, 0, 0),
}

local RARITY = {
    ["OG"]      = "rainbow",
    ["Secret"]  = Color3.fromRGB(220, 220, 220),
    ["God"]     = Color3.fromRGB(248, 113, 113),
    ["Burat"]   = Color3.fromRGB(74, 222, 128),
    ["Unknown"] = Color3.fromRGB(80, 80, 80),
}

local PING_NAMES = {
    ["Rosetti Tualetti"]=true,["Rosey and Teddy"]=true,["Los Amigos"]=true,
    ["Smurf Cat"]=true,["John Pork"]=true,["Dragon Cannelloni"]=true,
    ["Meowl"]=true,["Strawberry Elephant"]=true,["Ketchuru and Masturu"]=true,
    ["Ketupat Kepat"]=true,["Tang Tang Kelentang"]=true,["La Secret Combinasion"]=true,
    ["Los Bros"]=true,["Los Hotspotsitos"]=true,["Spaghetti Tualetti"]=true,
    ["Garama and Madundung"]=true,["Nuclearo Dinosauro"]=true,["Los Tacoritas"]=true,
    ["Tictac Sahur"]=true,["La Supreme Combinasion"]=true,["Los Combinasionas"]=true,
    ["La Casa Boo"]=true,["Los Spooky Combinasionas"]=true,["Reinito Sleighito"]=true,
    ["Sammyni Fattini"]=true,["Ketupat Bros"]=true,["Burguro And Fryuro"]=true,
    ["Lavadorito Spinito"]=true,["Hydra Dragon Cannelloni"]=true,["Dragon Gingerini"]=true,
    ["Cerberus"]=true,["Capitano Moby"]=true,["Headless Horseman"]=true,
}

local HIGH_INCOME_THRESHOLD = 500000000

-- ══════════════════════════════════════════
--  HELPERS
-- ══════════════════════════════════════════
local function getRequest()
    return (syn and syn.request) or (http and http.request) or http_request or request
end

local function short(n)
    n = tonumber(n) or 0
    local abs = math.abs(n)
    for _, s in ipairs({{1e12,"T"},{1e9,"B"},{1e6,"M"},{1e3,"K"}}) do
        if abs >= s[1] then
            return string.format("%.1f", n / s[1]):gsub("%.0$","") .. s[2]
        end
    end
    return tostring(n)
end

local function timeAgo(ts)
    local d = os.time() - ts
    if d < 0 then return "now" end
    if d < 60 then return d.."s"
    elseif d < 3600 then return math.floor(d/60).."m"
    elseif d < 86400 then return math.floor(d/3600).."h"
    else return math.floor(d/86400).."d" end
end

local function getTimeColor(ts)
    local d = (os.time() - ts) / 60
    if d >= 10 then return T.Red
    elseif d >= 5 then return T.Orange
    else return T.TextDim end
end

-- ══════════════════════════════════════════
--  UI HELPERS
-- ══════════════════════════════════════════
local function corner(p, r)
    local c = Instance.new("UICorner")
    c.CornerRadius = UDim.new(0, r or 4)
    c.Parent = p
    return c
end

local function stroke(p, color, thick, trans)
    local s = Instance.new("UIStroke")
    s.Color = color or T.Border
    s.Thickness = thick or 1
    s.Transparency = trans or 0
    s.Parent = p
    return s
end

local function lbl(parent, props)
    local l = Instance.new("TextLabel")
    l.BackgroundTransparency = 1
    l.TextXAlignment = props.align or Enum.TextXAlignment.Left
    l.Font = props.font or Enum.Font.Code
    l.TextSize = props.size or 12
    l.TextColor3 = props.color or T.Text
    l.Text = props.text or ""
    l.Size = props.sz or UDim2.new(1,0,1,0)
    l.Position = props.pos or UDim2.new(0,0,0,0)
    l.TextTruncate = props.trunc and Enum.TextTruncate.AtEnd or Enum.TextTruncate.None
    l.Parent = parent
    return l
end

local function frm(parent, props)
    local f = Instance.new("Frame")
    f.Size = props.sz or UDim2.new(1,0,0,32)
    f.Position = props.pos or UDim2.new(0,0,0,0)
    f.BackgroundColor3 = props.bg or T.Panel
    f.BorderSizePixel = 0
    f.Parent = parent
    return f
end

local function pill(parent, text, color, xOff, yOff, w)
    local p = frm(parent, {sz=UDim2.new(0,w or 52,0,18), pos=UDim2.new(1,xOff,0,yOff), bg=T.Bg})
    corner(p, 3)
    stroke(p, color, 1, 0.4)
    lbl(p, {text=text, size=10, color=color, font=Enum.Font.Code, align=Enum.TextXAlignment.Center})
    return p
end

-- ══════════════════════════════════════════
--  FETCH
-- ══════════════════════════════════════════
local function fetchGistData()
    if USE_MOCK_DATA then
        return {
            jobId="mock-bot", placeId=109983668079237, timestamp=os.time(), playerCount=0,
            brainrots={
                {name="Dragon Cannelloni",rarity="Secret",mutation="Rainbow",traits="Lightning",income=500000000,cost=250000000000,serverId="srv-abc123",foundAt=os.time()-60,playerCount=8},
                {name="Meowl",rarity="OG",mutation="Divine",traits="Meowl",income=800000000,cost=500000000000,serverId="srv-abc123",foundAt=os.time()-30,playerCount=8},
                {name="Ultra Rat",rarity="God",mutation="Cosmic",traits="Speed",income=120000000,cost=999000000000,serverId="srv-xyz789",foundAt=os.time()-10,playerCount=12},
                {name="Smurf Cat",rarity="Burat",mutation="Frost",traits="Chill",income=45000000,cost=50000000000,serverId="srv-xyz789",foundAt=os.time()-200,playerCount=12},
            }
        }
    end
    local req = getRequest()
    if not req then return nil end
    local ok, result = pcall(function()
        local res = req({
            Url = "https://api.github.com/gists/"..GIST_ID,
            Method = "GET",
            Headers = {
                ["Authorization"] = "token "..GITHUB_TOKEN,
                ["User-Agent"] = "Roblox-Brainrot-Viewer"
            }
        })
        if res.StatusCode == 200 then
            local data = HttpService:JSONDecode(res.Body)
            return HttpService:JSONDecode(data.files[GIST_FILENAME].content)
        end
    end)
    return ok and result or nil
end

-- ══════════════════════════════════════════
--  BUILD GUI
-- ══════════════════════════════════════════
local function buildGUI()
    local old = CoreGui:FindFirstChild("BrainrotViewerV3")
    if old then old:Destroy() end

    local sg = Instance.new("ScreenGui")
    sg.Name = "BrainrotViewerV3"
    sg.ResetOnSpawn = false
    sg.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

    -- ── Main Window (compact: 560 × 460) ───────
    local win = Instance.new("Frame")
    win.Name = "Window"
    win.Size = UDim2.new(0, 560, 0, 460)
    win.Position = UDim2.new(0.5, -280, 0.5, -230)
    win.BackgroundColor3 = T.Bg
    win.BorderSizePixel = 0
    win.Parent = sg
    corner(win, 8)
    stroke(win, T.BorderBright, 1, 0)

    -- ── Title Bar (34px) ───────────────────────
    local titleBar = Instance.new("TextButton")
    titleBar.Name = "TitleBar"
    titleBar.Size = UDim2.new(1,0,0,34)
    titleBar.BackgroundColor3 = T.Panel
    titleBar.BorderSizePixel = 0
    titleBar.Text = ""
    titleBar.AutoButtonColor = false
    titleBar.Parent = win
    corner(titleBar, 8)
    
                -- Bottom border separator
    local sep = frm(win, {sz=UDim2.new(1,0,0,1), pos=UDim2.new(0,0,0,34), bg=T.BorderBright})

    -- ── Tab Bar (28px) ────────────────────────
    local tabBar = Instance.new("Frame")
    tabBar.Name = "TabBar"
    tabBar.Size = UDim2.new(1,0,0,28)
    tabBar.Position = UDim2.new(0,0,0,35)
    tabBar.BackgroundColor3 = T.Panel
    tabBar.BorderSizePixel = 0
    tabBar.Parent = win

    local tabLayout = Instance.new("UIListLayout")
    tabLayout.FillDirection = Enum.FillDirection.Horizontal
    tabLayout.SortOrder = Enum.SortOrder.LayoutOrder
    tabLayout.Padding = UDim.new(0,1)
    tabLayout.Parent = tabBar

    local tabPad = Instance.new("UIPadding")
    tabPad.PaddingLeft = UDim.new(0,6)
    tabPad.PaddingTop = UDim.new(0,4)
    tabPad.Parent = tabBar

    local function createTab(name, order)
        local tab = Instance.new("TextButton")
        tab.Name = name
        tab.Size = UDim2.new(0,80,0,20)
        tab.BackgroundColor3 = T.Card
        tab.Text = name
        tab.TextColor3 = T.TextDim
        tab.TextSize = 11
        tab.Font = Enum.Font.Code
        tab.BorderSizePixel = 0
        tab.AutoButtonColor = false
        tab.LayoutOrder = order
        tab.Parent = tabBar
        corner(tab, 4)
        return tab
    end

    local serversTab = createTab("Servers", 1)
    local miscTab = createTab("Misc", 2)

    -- Tab separator line
    frm(win, {sz=UDim2.new(1,0,0,1), pos=UDim2.new(0,0,0,63), bg=T.BorderBright})

            -- Persistent status bar (below tab bar)
    local statusBar = Instance.new("Frame")
    statusBar.Name = "StatusBar"
    statusBar.Size = UDim2.new(1,-12,0,26)
    statusBar.Position = UDim2.new(0,6,0,67)
    statusBar.BackgroundColor3 = Color3.fromRGB(22,22,22)
    statusBar.BorderSizePixel = 0
    statusBar.ClipsDescendants = false
    statusBar.BackgroundTransparency = 0
    statusBar.ZIndex = 100
    statusBar.Parent = win
    corner(statusBar, 4)
    stroke(statusBar, T.BorderBright, 1, 0)

    local statusBarLbl = Instance.new("TextLabel")
    statusBarLbl.Name = "StatusBarLbl"
    statusBarLbl.BackgroundTransparency = 1
    statusBarLbl.Size = UDim2.new(1,-16,1,0)
    statusBarLbl.Position = UDim2.new(0,10,0,0)
    statusBarLbl.Font = Enum.Font.Code
    statusBarLbl.TextSize = 11
    statusBarLbl.TextColor3 = T.TextDim
    statusBarLbl.Text = "↻ Initializing..."
    statusBarLbl.TextXAlignment = Enum.TextXAlignment.Left
    statusBarLbl.ZIndex = 101
    statusBarLbl.Parent = statusBar

    -- Logo dot
    local logoDot = frm(titleBar, {sz=UDim2.new(0,8,0,8), pos=UDim2.new(0,12,0,13), bg=T.Green})
    corner(logoDot, 4)

    -- Title
    lbl(titleBar, {
        text="CLY NOTIFY", size=12, color=T.Text, font=Enum.Font.Code,
        sz=UDim2.new(0,180,1,0), pos=UDim2.new(0,28,0,0),
        align=Enum.TextXAlignment.Left
    })

            -- Status label — sits left of the chips cluster
    local statusLbl = Instance.new("TextLabel")
    statusLbl.Name = "StatusLbl"
    statusLbl.BackgroundTransparency = 1
    statusLbl.Size = UDim2.new(0,90,1,0)
    statusLbl.Position = UDim2.new(1,-320,0,0)
    statusLbl.Font = Enum.Font.Code
    statusLbl.TextSize = 10
    statusLbl.TextColor3 = T.TextDim
    statusLbl.Text = "· syncing..."
    statusLbl.TextXAlignment = Enum.TextXAlignment.Right
    statusLbl.Parent = titleBar

    -- Title bar right side layout (from right edge, clean spacing):
    --  [✕]       pos X: -28   width 22
    --  [–]       pos X: -56   width 22  (gap: 6px)
    --  [↺]       pos X: -84   width 22  (gap: 6px)
    --  divider   pos X: -96   width 1   (gap: 10px)
    --  chip "Servers" pos X: -192  width 90  (gap: 6px)
    --  divider   pos X: -204  width 1   (gap: 10px)
    --  chip "Total"   pos X: -300  width 90  (gap: 6px)

    -- Helper: small 22×22 action button with solid bg
    local function winBtn(xPos, sym, normalBg, hoverBg, normalCol, hoverCol)
        local b = Instance.new("TextButton")
        b.Size = UDim2.new(0,22,0,22)
        b.Position = UDim2.new(1,xPos,0,6)
        b.BackgroundColor3 = normalBg
        b.Text = sym
        b.TextColor3 = normalCol
        b.TextSize = 12
        b.Font = Enum.Font.GothamBold
        b.BorderSizePixel = 0
        b.AutoButtonColor = false
        b.Parent = titleBar
        corner(b, 4)
        b.MouseEnter:Connect(function()
            b.BackgroundColor3 = hoverBg
            b.TextColor3 = hoverCol
        end)
        b.MouseLeave:Connect(function()
            b.BackgroundColor3 = normalBg
            b.TextColor3 = normalCol
        end)
        return b
    end

        -- ✕ close — rightmost
    local closeBtn = winBtn(-28, "✕",
        Color3.fromRGB(48,18,18), Color3.fromRGB(239,68,68),
        Color3.fromRGB(239,68,68), Color3.fromRGB(255,255,255))

    -- – minimize
    local minimizeBtn = winBtn(-56, "–",
        Color3.fromRGB(30,30,30), Color3.fromRGB(50,50,50),
        Color3.fromRGB(160,160,160), Color3.fromRGB(220,220,220))

    -- ↺ resync
    local resyncBtn = winBtn(-84, "↺",
        Color3.fromRGB(18,30,20), Color3.fromRGB(20,50,25),
        Color3.fromRGB(74,222,128), Color3.fromRGB(134,239,172))
    resyncBtn.Name = "ResyncBtn"

    -- Divider before chips
    frm(titleBar, {sz=UDim2.new(0,1,0,16), pos=UDim2.new(1,-96,0,9), bg=T.BorderBright})

        -- Stat chips — each 90px wide
    local function tinyChip(icon, key, xOff, accentCol)
        local chip = frm(titleBar, {sz=UDim2.new(0,90,0,22), pos=UDim2.new(1,xOff,0,6), bg=T.Card})
        corner(chip, 4)
        stroke(chip, T.Border, 1, 0)
        local chipAccent = frm(chip, {sz=UDim2.new(0,2,0,12), pos=UDim2.new(0,0,0,5), bg=accentCol or T.AccentSub})
        corner(chipAccent, 1)
        lbl(chip, {text=icon, size=10, color=T.TextDim, font=Enum.Font.Code,
            sz=UDim2.new(0,20,1,0), pos=UDim2.new(0,6,0,0), align=Enum.TextXAlignment.Center})
        local val = Instance.new("TextLabel")
        val.Name = key
        val.BackgroundTransparency = 1
        val.Size = UDim2.new(1,-28,1,0)
        val.Position = UDim2.new(0,28,0,0)
        val.Font = Enum.Font.Code
        val.TextSize = 11
        val.TextColor3 = T.Text
        val.Text = "0"
        val.TextXAlignment = Enum.TextXAlignment.Left
        val.Parent = chip
        return val
    end

        local serverLbl = tinyChip("🖥", "ServerLbl", -192, T.Blue)
    frm(titleBar, {sz=UDim2.new(0,1,0,16), pos=UDim2.new(1,-204,0,9), bg=T.BorderBright})
    local totalLbl  = tinyChip("‼️", "TotalLbl",  -300, T.Green)

                    -- ── Servers Scroll Area ────────────────────────────
    local serversScroll = Instance.new("ScrollingFrame")
    serversScroll.Name = "ServersScroll"
    serversScroll.Size = UDim2.new(1,-2,1,-99)
    serversScroll.Position = UDim2.new(0,1,0,99)
    serversScroll.BackgroundTransparency = 1
    serversScroll.BorderSizePixel = 0
    serversScroll.ScrollBarThickness = 3
    serversScroll.ScrollBarImageColor3 = T.BorderBright
    serversScroll.CanvasSize = UDim2.new(0,0,0,0)
    serversScroll.AutomaticCanvasSize = Enum.AutomaticSize.Y
    serversScroll.ZIndex = 1
    serversScroll.Visible = true
    serversScroll.Parent = win

            local serversLayout = Instance.new("UIListLayout")
    serversLayout.SortOrder = Enum.SortOrder.LayoutOrder
    serversLayout.Padding = UDim.new(0,8)
    serversLayout.Parent = serversScroll

    local serversPad = Instance.new("UIPadding")
    serversPad.PaddingTop    = UDim.new(0,6)
    serversPad.PaddingBottom = UDim.new(0,6)
    serversPad.PaddingLeft   = UDim.new(0,6)
    serversPad.PaddingRight  = UDim.new(0,6)
    serversPad.Parent = serversScroll

    -- ── Misc Scroll Area ────────────────────────────
    local miscScroll = Instance.new("ScrollingFrame")
    miscScroll.Name = "MiscScroll"
    miscScroll.Size = UDim2.new(1,-2,1,-99)
    miscScroll.Position = UDim2.new(0,1,0,99)
    miscScroll.BackgroundTransparency = 1
    miscScroll.BorderSizePixel = 0
    miscScroll.ScrollBarThickness = 3
    miscScroll.ScrollBarImageColor3 = T.BorderBright
    miscScroll.CanvasSize = UDim2.new(0,0,0,0)
    miscScroll.AutomaticCanvasSize = Enum.AutomaticSize.Y
    miscScroll.ZIndex = 1
    miscScroll.Visible = false
    miscScroll.Parent = win

    local miscLayout = Instance.new("UIListLayout")
    miscLayout.SortOrder = Enum.SortOrder.LayoutOrder
    miscLayout.Padding = UDim.new(0,8)
    miscLayout.Parent = miscScroll

    local miscPad = Instance.new("UIPadding")
    miscPad.PaddingTop    = UDim.new(0,6)
    miscPad.PaddingBottom = UDim.new(0,6)
    miscPad.PaddingLeft   = UDim.new(0,6)
    miscPad.PaddingRight  = UDim.new(0,6)
    miscPad.Parent = miscScroll

    -- Misc tab: Current Job ID display
    local currentJobCard = Instance.new("Frame")
    currentJobCard.Size = UDim2.new(1,0,0,70)
    currentJobCard.BackgroundColor3 = T.Panel
    currentJobCard.BorderSizePixel = 0
    currentJobCard.LayoutOrder = 1
    currentJobCard.Parent = miscScroll
    corner(currentJobCard, 6)
    stroke(currentJobCard, T.Border, 1, 0)
    
    -- Card title
    lbl(currentJobCard, {
        text="Current Server Job ID",
        size=12, color=T.Text, font=Enum.Font.Code,
        sz=UDim2.new(1,-16,0,20), pos=UDim2.new(0,10,0,8),
        align=Enum.TextXAlignment.Left
    })
    
    -- Job ID display box (read-only, selectable)
    local currentJobIdBox = Instance.new("TextBox")
    currentJobIdBox.Name = "CurrentJobIdBox"
    currentJobIdBox.Size = UDim2.new(1,-20,0,28)
    currentJobIdBox.Position = UDim2.new(0,10,0,32)
    currentJobIdBox.BackgroundColor3 = T.Bg
    currentJobIdBox.BorderSizePixel = 0
    currentJobIdBox.Text = game.JobId
    currentJobIdBox.TextColor3 = T.Cyan
    currentJobIdBox.TextSize = 11
    currentJobIdBox.Font = Enum.Font.Code
    currentJobIdBox.TextXAlignment = Enum.TextXAlignment.Left
    currentJobIdBox.ClearTextOnFocus = false
    currentJobIdBox.TextEditable = false
    currentJobIdBox.Parent = currentJobCard
    corner(currentJobIdBox, 4)
    stroke(currentJobIdBox, T.Cyan, 1, 0.3)
    
    local currentJobPad = Instance.new("UIPadding")
    currentJobPad.PaddingLeft = UDim.new(0,8)
    currentJobPad.PaddingRight = UDim.new(0,8)
    currentJobPad.Parent = currentJobIdBox

        -- Misc tab: Join by Job ID section
    local joinCard = Instance.new("Frame")
    joinCard.Size = UDim2.new(1,0,0,80)
    joinCard.BackgroundColor3 = T.Panel
    joinCard.BorderSizePixel = 0
    joinCard.LayoutOrder = 2
    joinCard.Parent = miscScroll
    corner(joinCard, 6)
    stroke(joinCard, T.Border, 1, 0)
    
    -- Card title
    lbl(joinCard, {
        text="Join Server by Job ID",
        size=12, color=T.Text, font=Enum.Font.Code,
        sz=UDim2.new(1,-16,0,20), pos=UDim2.new(0,10,0,8),
        align=Enum.TextXAlignment.Left
    })
    
    -- Job ID input box
    local jobIdInput = Instance.new("TextBox")
    jobIdInput.Name = "JobIdInput"
    jobIdInput.Size = UDim2.new(1,-140,0,28)
    jobIdInput.Position = UDim2.new(0,10,0,36)
    jobIdInput.BackgroundColor3 = T.Bg
    jobIdInput.BorderSizePixel = 0
    jobIdInput.Text = ""
    jobIdInput.PlaceholderText = "Enter Job ID..."
    jobIdInput.PlaceholderColor3 = T.TextDim
    jobIdInput.TextColor3 = T.Text
    jobIdInput.TextSize = 11
    jobIdInput.Font = Enum.Font.Code
    jobIdInput.TextXAlignment = Enum.TextXAlignment.Left
    jobIdInput.ClearTextOnFocus = false
    jobIdInput.Parent = joinCard
    corner(jobIdInput, 4)
    stroke(jobIdInput, T.Border, 1, 0)
    
    local inputPad = Instance.new("UIPadding")
    inputPad.PaddingLeft = UDim.new(0,8)
    inputPad.PaddingRight = UDim.new(0,8)
    inputPad.Parent = jobIdInput
    
    -- Join button
    local joinBtn = Instance.new("TextButton")
    joinBtn.Name = "JoinBtn"
    joinBtn.Size = UDim2.new(0,120,0,28)
    joinBtn.Position = UDim2.new(1,-130,0,36)
    joinBtn.BackgroundColor3 = Color3.fromRGB(18,30,20)
    joinBtn.Text = "JOIN →"
    joinBtn.TextColor3 = T.Green
    joinBtn.TextSize = 11
    joinBtn.Font = Enum.Font.Code
    joinBtn.BorderSizePixel = 0
    joinBtn.AutoButtonColor = false
    joinBtn.Parent = joinCard
    corner(joinBtn, 4)
    stroke(joinBtn, T.GreenDim, 1, 0)
    
        joinBtn.MouseEnter:Connect(function()
        joinBtn.BackgroundColor3 = Color3.fromRGB(20,50,25)
        joinBtn.TextColor3 = Color3.fromRGB(134,239,172)
    end)
    joinBtn.MouseLeave:Connect(function()
        joinBtn.BackgroundColor3 = Color3.fromRGB(18,30,20)
        joinBtn.TextColor3 = T.Green
    end)

    -- Tab switching logic
    local function switchTab(tabName)
        if tabName == "Servers" then
            serversScroll.Visible = true
            miscScroll.Visible = false
            serversTab.BackgroundColor3 = T.Bg
            serversTab.TextColor3 = T.Text
            miscTab.BackgroundColor3 = T.Card
            miscTab.TextColor3 = T.TextDim
        else
            serversScroll.Visible = false
            miscScroll.Visible = true
            serversTab.BackgroundColor3 = T.Card
            serversTab.TextColor3 = T.TextDim
            miscTab.BackgroundColor3 = T.Bg
            miscTab.TextColor3 = T.Text
        end
    end

    serversTab.MouseButton1Click:Connect(function() switchTab("Servers") end)
    miscTab.MouseButton1Click:Connect(function() switchTab("Misc") end)

    serversTab.MouseEnter:Connect(function()
        if serversScroll.Visible then return end
        serversTab.BackgroundColor3 = T.CardHover
    end)
    serversTab.MouseLeave:Connect(function()
        if serversScroll.Visible then return end
        serversTab.BackgroundColor3 = T.Card
    end)

    miscTab.MouseEnter:Connect(function()
        if miscScroll.Visible then return end
        miscTab.BackgroundColor3 = T.CardHover
    end)
    miscTab.MouseLeave:Connect(function()
        if miscScroll.Visible then return end
        miscTab.BackgroundColor3 = T.Card
    end)

    -- Set initial tab
    switchTab("Servers")

    sg.Parent = CoreGui

                return {
        gui=sg, win=win, titleBar=titleBar, serversScroll=serversScroll, miscScroll=miscScroll,
        statusLbl=statusLbl, totalLbl=totalLbl, serverLbl=serverLbl,
        closeBtn=closeBtn, minimizeBtn=minimizeBtn, resyncBtn=resyncBtn,
        statusBar=statusBar, statusBarLbl=statusBarLbl,
        serversTab=serversTab, miscTab=miscTab,
        jobIdInput=jobIdInput, joinBtn=joinBtn,
    }
end

-- ══════════════════════════════════════════
--  SERVER PANEL
-- ══════════════════════════════════════════
local function buildServerPanel(serverId, brainrots, placeId, parent, order)
    table.sort(brainrots, function(a,b)
        local aPing = PING_NAMES[a.name] and 1 or 0
        local bPing = PING_NAMES[b.name] and 1 or 0
        if aPing ~= bPing then return aPing > bPing end
        return (a.income or 0) > (b.income or 0)
    end)

    local isCurrentServer = (serverId == game.JobId)
    local playerCount = brainrots[1] and brainrots[1].playerCount or 0
    local shortId = serverId:sub(1,14).."…"

        local isHighlighted = false
    for _, br in ipairs(brainrots) do
        if (br.income or 0) >= HIGH_INCOME_THRESHOLD or PING_NAMES[br.name] then
            isHighlighted = true; break
        end
    end

    -- Wrapper
    local wrapper = Instance.new("Frame")
    wrapper.Size = UDim2.new(1,0,0,0)
    wrapper.AutomaticSize = Enum.AutomaticSize.Y
    wrapper.BackgroundTransparency = 1
    wrapper.BorderSizePixel = 0
    wrapper.LayoutOrder = order
    wrapper.Parent = parent

    -- Panel card
    local panel = Instance.new("Frame")
    panel.Size = UDim2.new(1,0,0,0)
    panel.AutomaticSize = Enum.AutomaticSize.Y
    panel.BackgroundColor3 = T.Panel
    panel.BorderSizePixel = 0
    panel.Parent = wrapper
    corner(panel, 6)
    
        local borderColor = isHighlighted and T.Gold or (isCurrentServer and T.Cyan or T.Border)
    stroke(panel, borderColor, 1, 0)
    
    -- Add smooth glow effect for highlighted servers
    if isHighlighted then
        local glow = Instance.new("UIStroke")
        glow.Color = T.Gold
        glow.Thickness = 3
        glow.Transparency = 0.7
        glow.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
        glow.Parent = panel
    end

    local panelLayout = Instance.new("UIListLayout")
    panelLayout.SortOrder = Enum.SortOrder.LayoutOrder
    panelLayout.Padding = UDim.new(0,0)
    panelLayout.Parent = panel

    -- ── Server Header (28px) ──────────────────
    local header = Instance.new("TextButton")
    header.Size = UDim2.new(1,0,0,28)
    header.BackgroundColor3 = T.Card
    header.BorderSizePixel = 0
    header.Text = ""
    header.AutoButtonColor = false
    header.LayoutOrder = 1
    header.Parent = panel
    corner(header, 6)

    -- Left accent strip (2px)
    local strip = frm(header, {sz=UDim2.new(0,2,0,16), pos=UDim2.new(0,8,0,6),
        bg=isHighlighted and T.Gold or (isCurrentServer and T.Cyan or T.AccentSub)})
    corner(strip, 1)

        -- Server ID (takes most of the left space)
    lbl(header, {
        text=(isHighlighted and "★ " or "")..shortId,
        size=11, color=isHighlighted and T.Gold or T.TextMid, font=Enum.Font.Code,
        sz=UDim2.new(1,-260,1,0), pos=UDim2.new(0,18,0,0),
        align=Enum.TextXAlignment.Left, trunc=true
    })

        -- Right-side cluster: [👥N] [+N] [separator] [▾] [JOIN/HERE]
    -- Layout from right: join@-60, chevron@-86, separator@-96, found@-138, players@-184

        -- Player count pill
    local plrPill = frm(header, {sz=UDim2.new(0,42,0,18), pos=UDim2.new(1,-184,0,5), bg=T.Bg})
    corner(plrPill, 3)
    stroke(plrPill, T.Border, 1, 0)
    lbl(plrPill, {text="👥"..playerCount, size=10, color=T.TextDim, font=Enum.Font.Code, align=Enum.TextXAlignment.Center})

    -- Found count pill
    local foundPill = frm(header, {sz=UDim2.new(0,38,0,18), pos=UDim2.new(1,-138,0,5), bg=T.Bg})
    corner(foundPill, 3)
    stroke(foundPill, T.GreenDim, 1, 0)
    lbl(foundPill, {text="+"..#brainrots, size=10, color=T.Green, font=Enum.Font.Code, align=Enum.TextXAlignment.Center})

    -- Thin separator before join area
    frm(header, {sz=UDim2.new(0,1,0,14), pos=UDim2.new(1,-96,0,7), bg=T.BorderBright})

    -- Chevron toggle
    local isOpen = true
    local chevron = lbl(header, {
        text="▾", size=13, color=T.TextDim, font=Enum.Font.Code,
        sz=UDim2.new(0,18,1,0), pos=UDim2.new(1,-86,0,0),
        align=Enum.TextXAlignment.Center
    })

            if isCurrentServer then
        local here = frm(header, {sz=UDim2.new(0,54,0,18), pos=UDim2.new(1,-60,0,5), bg=T.Bg})
        corner(here, 3)
        stroke(here, T.Cyan, 1, 0.3)
        lbl(here, {text="● HERE", size=9, color=T.Cyan, font=Enum.Font.Code, align=Enum.TextXAlignment.Center})
    else
        local joinBtn = Instance.new("TextButton")
        joinBtn.Size = UDim2.new(0,54,0,18)
        joinBtn.Position = UDim2.new(1,-60,0,5)
        joinBtn.BackgroundColor3 = Color3.fromRGB(22,22,22)
        joinBtn.Text = "JOIN →"
        joinBtn.TextColor3 = T.TextMid
        joinBtn.TextSize = 10
        joinBtn.Font = Enum.Font.Code
        joinBtn.BorderSizePixel = 0
        joinBtn.AutoButtonColor = false
        joinBtn.Parent = header
        corner(joinBtn, 3)
        stroke(joinBtn, T.BorderBright, 1, 0)
        joinBtn.MouseEnter:Connect(function()
            joinBtn.BackgroundColor3 = Color3.fromRGB(34,34,34)
            joinBtn.TextColor3 = T.Green
        end)
        joinBtn.MouseLeave:Connect(function()
            joinBtn.BackgroundColor3 = Color3.fromRGB(22,22,22)
            joinBtn.TextColor3 = T.TextMid
        end)
        joinBtn.MouseButton1Click:Connect(function()
            joinBtn.Text = "..."
            pcall(function()
                TeleportService:TeleportToPlaceInstance(placeId, serverId, Players.LocalPlayer)
            end)
        end)
    end

    -- ── Body ──────────────────────────────────
    local body = Instance.new("Frame")
    body.Size = UDim2.new(1,0,0,0)
    body.AutomaticSize = Enum.AutomaticSize.Y
    body.BackgroundTransparency = 1
    body.BorderSizePixel = 0
    body.LayoutOrder = 2
    body.ClipsDescendants = false
    body.Parent = panel

    local bodyPad = Instance.new("UIPadding")
    bodyPad.PaddingLeft   = UDim.new(0,6)
    bodyPad.PaddingRight  = UDim.new(0,6)
    bodyPad.PaddingTop    = UDim.new(0,3)
    bodyPad.PaddingBottom = UDim.new(0,6)
    bodyPad.Parent = body

    local bodyLayout = Instance.new("UIListLayout")
    bodyLayout.SortOrder = Enum.SortOrder.LayoutOrder
    bodyLayout.Padding = UDim.new(0,2)
    bodyLayout.Parent = body

    -- ── Brainrot Rows (compact 52px each) ─────
    for i, br in ipairs(brainrots) do
        local rarityColor = RARITY[br.rarity] or RARITY["Unknown"]
        local isRainbow = (rarityColor == "rainbow")
        local isPing = PING_NAMES[br.name]

        local row = Instance.new("Frame")
        row.Size = UDim2.new(1,0,0,52)
        row.BackgroundColor3 = T.Bg
        row.BorderSizePixel = 0
        row.LayoutOrder = i
        row.Parent = body
        corner(row, 4)
        stroke(row, isPing and Color3.fromRGB(40,40,60) or T.Border, 1, 0)

        -- Ping dot (bright yellow, top-left corner)
        if isPing then
            local dot = frm(row, {sz=UDim2.new(0,6,0,6), pos=UDim2.new(0,4,0,4), bg=Color3.fromRGB(250,204,21)})
            corner(dot, 4)
            -- glow stroke
            local dotStroke = Instance.new("UIStroke")
            dotStroke.Color = Color3.fromRGB(253,224,71)
            dotStroke.Thickness = 1.5
            dotStroke.Transparency = 0.3
            dotStroke.Parent = dot
        end

        -- Left rarity bar (2px)
        local rarBar = frm(row, {sz=UDim2.new(0,2,0,36), pos=UDim2.new(0,6,0,8),
            bg=isRainbow and Color3.fromRGB(255,0,200) or rarityColor})
        corner(rarBar, 1)
        if isRainbow then
            local rg = Instance.new("UIGradient")
            rg.Color = ColorSequence.new({
                ColorSequenceKeypoint.new(0.0,  Color3.fromRGB(255,0,0)),
                ColorSequenceKeypoint.new(0.25, Color3.fromRGB(255,200,0)),
                ColorSequenceKeypoint.new(0.5,  Color3.fromRGB(0,255,100)),
                ColorSequenceKeypoint.new(0.75, Color3.fromRGB(0,100,255)),
                ColorSequenceKeypoint.new(1.0,  Color3.fromRGB(200,0,255)),
            })
            rg.Rotation = 90
            rg.Parent = rarBar
        end

                -- ── Row line 1: Name + rarity + time ──
        lbl(row, {
            text=br.name, size=12, color=T.Text, font=Enum.Font.Code,
            sz=UDim2.new(1,-160,0,16), pos=UDim2.new(0,16,0,6),
            align=Enum.TextXAlignment.Left, trunc=true
        })

        -- Rarity badge (right side)
        local rCol = isRainbow and Color3.fromRGB(200,100,255) or rarityColor
        local rb = frm(row, {sz=UDim2.new(0,54,0,15), pos=UDim2.new(1,-108,0,7), bg=T.Bg})
        corner(rb, 3)
        stroke(rb, rCol, 1, 0.3)
        local rLbl = lbl(rb, {
            text=br.rarity, size=9, color=rCol, font=Enum.Font.Code,
            align=Enum.TextXAlignment.Center
        })
        if isRainbow then
            local rg2 = Instance.new("UIGradient")
            rg2.Color = ColorSequence.new({
                ColorSequenceKeypoint.new(0,   Color3.fromRGB(255,80,80)),
                ColorSequenceKeypoint.new(0.5, Color3.fromRGB(80,255,180)),
                ColorSequenceKeypoint.new(1,   Color3.fromRGB(180,80,255)),
            })
            rg2.Parent = rLbl
        end

        -- Time ago
        lbl(row, {
            text=timeAgo(br.foundAt), size=10, color=getTimeColor(br.foundAt), font=Enum.Font.Code,
            sz=UDim2.new(0,46,0,15), pos=UDim2.new(1,-48,0,8),
            align=Enum.TextXAlignment.Right
        })

        -- ── Row line 2: Income | Cost | Mutation | Traits ──
        local line2 = frm(row, {sz=UDim2.new(1,-16,0,16), pos=UDim2.new(0,16,0,28), bg=T.Transparent})
        line2.BackgroundTransparency = 1

        local l2layout = Instance.new("UIListLayout")
        l2layout.FillDirection = Enum.FillDirection.Horizontal
        l2layout.VerticalAlignment = Enum.VerticalAlignment.Center
        l2layout.Padding = UDim.new(0,0)
        l2layout.Parent = line2

                local function stat2(icon, val, col, w)
            local s = frm(line2, {sz=UDim2.new(0,w,1,0), bg=T.Transparent})
            s.BackgroundTransparency = 1
            lbl(s, {text=icon..val, size=10, color=col, font=Enum.Font.Code, align=Enum.TextXAlignment.Left})
        end

        stat2("$", short(br.income).."/s", T.Green, 105)
        stat2("¢", short(br.cost),          T.Yellow, 90)
        stat2("◈", br.mutation or "—",      T.TextDim, 95)
        stat2("◎", br.traits or "—",        T.TextDim, 150)
    end

    -- ── Dropdown toggle ───────────────────────
    local function setOpen(open)
        isOpen = open
        body.Visible = open
        chevron.Text = open and "▾" or "▸"
        chevron.TextColor3 = open and T.TextMid or T.TextDim
    end

    header.MouseButton1Click:Connect(function() setOpen(not isOpen) end)
    header.MouseEnter:Connect(function() header.BackgroundColor3 = T.CardHover end)
    header.MouseLeave:Connect(function() header.BackgroundColor3 = T.Card end)

    return wrapper
end

-- ══════════════════════════════════════════
--  UPDATE GUI
-- ══════════════════════════════════════════
local function updateGUI(ui, data)
    if not data then
        ui.statusLbl.Text = "· error"
        ui.statusLbl.TextColor3 = T.Red
        return
    end

        for _, ch in ipairs(ui.serversScroll:GetChildren()) do
        if ch:IsA("Frame") then ch:Destroy() end
    end

    local groups = {}
    for _, br in ipairs(data.brainrots or {}) do
        local sid = br.serverId or "unknown"
        groups[sid] = groups[sid] or {}
        table.insert(groups[sid], br)
    end

    local total, serverCount = #(data.brainrots or {}), 0
    for _ in pairs(groups) do serverCount = serverCount + 1 end

    ui.totalLbl.Text  = tostring(total)
    ui.serverLbl.Text = tostring(serverCount).." srv"

    local sorted = {}
    for sid, brs in pairs(groups) do
        local t = 0
        for _, b in ipairs(brs) do if b.foundAt > t then t = b.foundAt end end
        table.insert(sorted, {id=sid, brs=brs, latest=t})
    end
    table.sort(sorted, function(a,b) return a.latest > b.latest end)

        for idx, s in ipairs(sorted) do
        buildServerPanel(s.id, s.brs, data.placeId, ui.serversScroll, idx)
    end
end

-- ══════════════════════════════════════════
--  MAIN
-- ══════════════════════════════════════════
local function main()
    local ui = buildGUI()
    local TeleportService = game:GetService("TeleportService")

    -- Single shared syncedAt — updated whenever we successfully sync
    local syncedAt = os.time()
    local isSyncing = false
    local lastAutoSync = os.time()
    local AUTO_RESYNC_INTERVAL = 30
    local currentPlaceId = 109983668079237  -- Default place ID

    -- Status bar helper: update persistent status message (DEFINE EARLY)
    local function updateStatusBar(msg, col)
        ui.statusBarLbl.Text = msg
        ui.statusBarLbl.TextColor3 = col or T.TextMid
    end

    ui.closeBtn.MouseButton1Click:Connect(function()
        ui.gui:Destroy()
    end)

    -- Join by Job ID functionality (same mechanism as server panel join buttons)
    ui.joinBtn.MouseButton1Click:Connect(function()
        local job = ui.jobIdInput.Text:gsub("%s+", "")
        if job == "" then
            updateStatusBar("✕ Missing Job ID — Paste one first", T.Red)
            return
        end
        
        ui.joinBtn.Text = "..."
        updateStatusBar("↻ Joining server...", T.Yellow)
        
        task.wait(0.1)
        pcall(function()
            TeleportService:TeleportToPlaceInstance(currentPlaceId, job, Players.LocalPlayer)
        end)
    end)

    local minimized = false
    ui.minimizeBtn.MouseButton1Click:Connect(function()
        minimized = not minimized
        ui.serversScroll.Visible = not minimized and ui.serversScroll.Visible
        ui.miscScroll.Visible = not minimized and ui.miscScroll.Visible
        ui.win.Size = minimized
            and UDim2.new(0,560,0,36)
            or  UDim2.new(0,560,0,460)
        ui.minimizeBtn.Text = minimized and "□" or "—"
    end)

    -- One persistent ticker that reads syncedAt — never respawned
    task.spawn(function()
        while ui.gui.Parent do
            task.wait(1)
            if not ui.gui.Parent then break end
            local d = os.time() - syncedAt
            if d < 5 then
                ui.statusLbl.Text = "· just now"
                ui.statusLbl.TextColor3 = T.Green
            elseif d < 60 then
                ui.statusLbl.Text = "· "..d.."s ago"
                ui.statusLbl.TextColor3 = d < 25 and T.Green or T.TextDim
            elseif d < 3600 then
                ui.statusLbl.Text = "· "..math.floor(d/60).."m ago"
                ui.statusLbl.TextColor3 = T.Orange
            else
                ui.statusLbl.Text = "· "..math.floor(d/3600).."h ago"
                ui.statusLbl.TextColor3 = T.Red
            end
        end
    end)

        local function doSync(force)
        if isSyncing then return end
        isSyncing = true
        ui.resyncBtn.Text = "↻"
        ui.resyncBtn.TextColor3 = T.Yellow
        ui.resyncBtn.BackgroundColor3 = Color3.fromRGB(40,35,10)
        updateStatusBar("↻ Syncing...", T.Yellow)

        local prevTimestamp = lastKnownTimestamp
        local newData = fetchGistData()
        -- Always reset the timer on every sync attempt
        syncedAt = os.time()

                if ui.gui.Parent and newData then
            if newData.timestamp ~= prevTimestamp then
                lastKnownTimestamp = newData.timestamp
                local newCount = #(newData.brainrots or {})
                currentPlaceId = newData.placeId or currentPlaceId  -- Update place ID
                updateGUI(ui, newData)
                updateStatusBar("✓ Synced — " .. newCount .. " brainrot(s) found", T.Green)
            else
                updateStatusBar("— No new brainrots since last sync", T.TextDim)
            end
        else
            updateStatusBar("✕ Sync failed — could not reach Gist", T.Red)
        end

        ui.resyncBtn.Text = "↺"
        ui.resyncBtn.TextColor3 = Color3.fromRGB(74,222,128)
        ui.resyncBtn.BackgroundColor3 = Color3.fromRGB(18,30,20)
        isSyncing = false
        lastAutoSync = os.time()
    end

    -- Initial load (suppress toast on first run)
    local isFirstSync = true
    doSync(true)
    isFirstSync = false

    -- Manual resync: cancel any ongoing sync so manual always wins
    ui.resyncBtn.MouseButton1Click:Connect(function()
        isSyncing = false  -- force unlock so manual click is never dropped
        task.spawn(function() doSync(true) end)
    end)

        -- Auto loop: check every REFRESH_INTERVAL (5s), force-resync every 30s
    task.spawn(function()
        while ui.gui.Parent do
            task.wait(REFRESH_INTERVAL)
            if not ui.gui.Parent then break end
            local shouldForce = (os.time() - lastAutoSync) >= AUTO_RESYNC_INTERVAL
            if shouldForce then
                task.spawn(function() doSync(true) end)
            end
        end
    end)

        -- Draggable (mobile + desktop)
    local dragging, dragStart, winStart
    ui.titleBar.InputBegan:Connect(function(inp)
        if inp.UserInputType == Enum.UserInputType.MouseButton1 or inp.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            dragStart = inp.Position
            winStart = ui.win.Position
            inp.Changed:Connect(function()
                if inp.UserInputState == Enum.UserInputState.End then dragging = false end
            end)
        end
    end)
    local lastDrag
    ui.titleBar.InputChanged:Connect(function(inp)
        if inp.UserInputType == Enum.UserInputType.MouseMovement or inp.UserInputType == Enum.UserInputType.Touch then
            lastDrag = inp
        end
    end)
    UserInputService.InputChanged:Connect(function(inp)
        if inp == lastDrag and dragging then
            local delta = inp.Position - dragStart
            ui.win.Position = UDim2.new(
                winStart.X.Scale, winStart.X.Offset + delta.X,
                winStart.Y.Scale, winStart.Y.Offset + delta.Y
            )
        end
    end)
end

main()
